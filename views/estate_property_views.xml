<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Properties Action with Default Search Filter -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <!-- Chapter 11: Add default filter for available properties -->
        <field name="context">{'search_default_available': 1}</field>
    </record>

    <!-- Property Types Action -->
    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Property Tags Action -->
    <record id="estate_property_tag_action" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Chapter 11: Stat Button Target Action for Property Offers -->
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

    <!-- Property List View with Color Decorations -->
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <!-- Chapter 11: Added decoration attributes for different states -->
            <list string="Properties" decoration-success="state in ('offer_received', 'offer_accepted')" decoration-bf="state == 'offer_accepted'" decoration-muted="state == 'sold'">
                <field name="name" string="Title"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="selling_price" optional="show"/>
                <field name="bedrooms" optional="hide"/>
                <field name="living_area" optional="hide"/>
                <!-- Chapter 11: Make date availability hidden by default -->
                <field name="date_availability" optional="hide"/>
                <!-- Chapter 11: Required for list decorations to work properly -->
                <field name="state" invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Property Form View -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_set_sold" type="object" string="Sold" class="oe_highlight" invisible="state in ('sold', 'canceled')"/>
                    <button name="action_set_canceled" type="object" string="Cancel" invisible="state in ('sold', 'canceled')"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Property Title"/>
                        </h1>
                    </div>
                    <p>
                        <!-- Chapter 11: Added color options for tags picker -->
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" placeholder="Tags..."/>
                    </p>
                    <group>
                        <group>
                            <!-- Chapter 11: Prevent quick creation and editing of property types from here -->
                            <field name="property_type_id" options="{'no_create': True, 'no_edit': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <group string="Property Features">
                                <group>
                                    <field name="description"/>
                                    <field name="bedrooms"/>
                                    <field name="living_area"/>
                                    <!-- Chapter 11: Conditional display based on garden field -->
                                    <field name="garden_area" invisible="not garden"/>
                                    <field name="total_area"/>
                                    <field name="facades"/>
                                </group>
                                <group>
                                    <field name="garden"/>
                                    <!-- Chapter 11: Conditional display based on garden field -->
                                    <field name="garden_orientation" invisible="not garden"/>
                                    <field name="garage"/>
                                    <field name="active"/>
                                </group>
                            </group>
                        </page>
                        <page string="Offers">
                            <!-- Chapter 11: Make the list readonly once the property state is accepted, sold, or canceled -->
                            <field name="offer_ids" readonly="state in ('offer_accepted', 'sold', 'canceled')">
                                <list string="Offers" editable="bottom" decoration-success="status=='accepted'" decoration-danger="status=='refused'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <!-- Chapter 11: Hide buttons once the offer status is set -->
                                    <button name="action_accept_offer" type="object" icon="fa-check" title="Accept" invisible="status"/>
                                    <button name="action_refuse_offer" type="object" icon="fa-times" title="Refuse" invisible="status"/>
                                    <!-- Chapter 11: Hide status field column from list view as requested -->
                                    <field name="status" invisible="1"/>
                                </list>
                            </field>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="seller_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Property Search View -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <!-- Chapter 11: Overrode domain to search for living area equal or greater than value -->
                <field name="living_area" string="Living Area (>=)" filter_domain="[('living_area', '>=', self)]"/>
                <separator/>
                <filter name="available" string="Available" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                <separator/>
                <filter name="group_by_postcode" string="Group by Postcode" context="{'group_by': 'postcode'}"/>
            </search>
        </field>
    </record>

    <!-- Property Types List View with Handle Widget -->
    <record id="estate_property_type_view_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Property Types">
                <!-- Chapter 11: Drag and drop handle widget for manual sequencing -->
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- Chapter 11: Form View for Property Types with Stat Button and Inline View -->
    <record id="estate_property_type_view_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <sheet>
                    <!-- Chapter 11: Smart Stat Button Box -->
                    <div class="oe_button_box" name="button_box">
                        <button name="%(estate_property_offer_action)d" type="action" class="oe_stat_button" icon="fa-money">
                            <field name="offer_count" string="Offers" widget="statinfo"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <!-- Chapter 11: Inline List View displaying linked properties -->
                    <notebook>
                        <page string="Properties">
                            <field name="property_ids">
                                <list string="Properties" create="false" edit="false">
                                    <field name="name" string="Title"/>
                                    <field name="expected_price"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Property Tags List View with Editable List Feature -->
    <record id="estate_property_tag_view_list" model="ir.ui.view">
        <field name="name">estate.property.tag.list</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <!-- Chapter 11: Make property tag list view editable on-the-fly -->
            <list string="Property Tags" editable="bottom">
                <field name="name"/>
                <field name="color" widget="color_picker"/>
            </list>
        </field>
    </record>

    <!-- Chapter 12: Inherit base view_users_form to add properties notebook page -->
    <record id="res_users_view_form_inherit" model="ir.ui.view">
        <field name="name">res.users.form.inherit.estate</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <!-- Locate the notebook element in the parent view to append a new page -->
            <xpath expr="//notebook" position="inside">
                <page string="Real Estate Properties">
                    <field name="property_ids"/>
                </page>
            </xpath>
        </field>
    </record>

</odoo>

